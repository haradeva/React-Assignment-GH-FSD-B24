import React from "react";

export default function StyledComponent() {
  const style = {
    backgroundColor: "lightblue",
    color: "white",
  };
  return <div style={style}>Hello, this is a styled component.</div>;
}
